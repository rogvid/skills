# Design docs, kept local while the repo's home is still being decided.
.scratch/
/docs/

# Node / TypeScript build artifacts (bundled skill CLIs)
node_modules/
dist/
build/

# Python
__pycache__/

# demo-video: the video and working files are regenerated by re-running record.py
demo.mp4
sheet.png
timeline.md
failure.png
.take/
.tts/

.env

# Agent worktrees
.claude/

# prek/pre-commit caches
.prek-cache/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
